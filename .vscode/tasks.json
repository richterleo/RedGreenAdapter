{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "accelerateSetup",
            "type": "shell",
            "command": "accelerate launch --config_file /root/RedGreenAdapter/single_gpu_config.yml ${file}",
            "problemMatcher": []
        }
    ]
}